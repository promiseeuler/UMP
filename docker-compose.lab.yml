name: ump-hardware-readiness

services:
  core:
    build:
      context: .
      dockerfile: docker/Dockerfile.lab
    command: ["ump-lab", "run", "--workspace", "/evidence/run", "--output", "/evidence/scenario.json"]
    volumes:
      - ./.ump-lab:/evidence
    profiles: ["core"]

  mqtt:
    image: eclipse-mosquitto:2.0.22
    command: ["mosquitto", "-c", "/mosquitto/config/lab.conf"]
    volumes:
      - ./config/mosquitto-lab.conf:/mosquitto/config/lab.conf:ro
    ports:
      - "1883:1883"
    profiles: ["standards", "fault"]
    healthcheck:
      test: ["CMD", "mosquitto_sub", "-h", "127.0.0.1", "-t", "$$SYS/broker/uptime", "-C", "1", "-W", "2"]
      interval: 2s
      timeout: 3s
      retries: 10

  standards:
    build:
      context: .
      dockerfile: docker/Dockerfile.lab
    command: ["python", "-m", "ump.lab_services", "vda5050", "--mqtt-endpoint", "mqtt:1883"]
    depends_on:
      mqtt:
        condition: service_healthy
    profiles: ["standards"]

  fault-proxy:
    image: ghcr.io/shopify/toxiproxy:2.12.0
    ports:
      - "8474:8474"
      - "1884:1884"
    profiles: ["fault"]

  fault-test:
    build:
      context: .
      dockerfile: docker/Dockerfile.lab
    command: ["sh", "-c", "python -m ump.lab_services toxiproxy --toxiproxy-api http://fault-proxy:8474 && python -m ump.lab_services vda5050 --mqtt-endpoint fault-proxy:1884"]
    depends_on:
      mqtt:
        condition: service_healthy
      fault-proxy:
        condition: service_started
    profiles: ["fault"]

  ros2:
    build:
      context: .
      dockerfile: docker/Dockerfile.ros2
    command: ["python3", "-m", "ump.lab_services", "ros2"]
    profiles: ["standards"]

  soak:
    build:
      context: .
      dockerfile: docker/Dockerfile.lab
    command: ["ump-lab", "soak", "--duration-s", "28800", "--participants", "25", "--state-hz", "2"]
    profiles: ["soak"]

  visual:
    build:
      context: .
      dockerfile: docker/Dockerfile.gazebo
    command: ["gz", "sim", "-s", "-r", "--iterations", "10", "/opt/ump/gazebo/warehouse.sdf"]
    profiles: ["visual"]
