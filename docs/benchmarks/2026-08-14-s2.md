# Phase 2 S2 Exit Evidence

**Evidence class:** Phase 2 development exit gate  
**Date:** 2026-08-14  
**Source state:** `d2d8fa0` plus uncommitted Phase 0 through Phase 2 implementation  
**Commands:** `make check`, `make sim-s0`, `make sim-s1`, `make sim-s2`

## Scenario result

S2 covers the authorized delivery baseline plus cancellation before and during execution, duplicate delivery, bounded retry/backoff, expired command, executor crash, network partition, lease expiry, lease revocation, coordinator restart, conflicting issuer, and a non-idempotent unknown outcome.

| Measurement | Result |
|---|---:|
| Scenario variants | 13 |
| Physical-action surrogates | 9 |
| Duplicate physical actions | 0 |
| Unauthorized executions | 0 |
| Sampled correlated trace events | 11 |
| S2 result | Passed |

The partitioned coordinator reconciles through the runtime state machine, preserves the executor's authoritative revision, and reloads the converged terminal record from its journal after restart. A conflicting terminal result becomes `unknown` and requires inspection.

## Automated coverage

- 39 Rust tests pass across CLI, protocol, runtime, simulation, and transport crates.
- Seven Python tests pass for wire vectors and sync/async handler registration.
- 2,048 randomized byte buffers exercise protobuf decoding without panic.
- 512 randomized bounded/oversized task requests exercise validation, retry policy, and attempt limits without panic.
- A loopback integration test uses separate certificate roots to negotiate QUIC, submit and durably acknowledge a task, reconcile it, cancel it, and reload the journal.

## Platform and footprint

The current tree passed workspace tests and release S2 on emulated Linux ARM64 and AMD64 with Rust 1.85.1. Local release-mode `umpd`, configured with a registered task capability and durable journal, measured:

| Measurement | Result |
|---|---:|
| Idle RSS after five seconds | 3,360 KiB |
| Idle CPU | 0.0% |
| `umpd` binary | 3,950,432 bytes |
| `ump` binary | 4,918,048 bytes |

These results are development evidence. Native onboard Linux hardware, physical networks, production credentials, long-running load, independent fuzzing, and security review remain mandatory before deployment claims.
