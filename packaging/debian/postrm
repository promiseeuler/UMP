#!/bin/sh
set -e

if command -v systemctl >/dev/null 2>&1; then
    systemctl daemon-reload >/dev/null 2>&1 || true
fi

# Identity, credentials, and journals intentionally survive package removal.
# Administrators may remove /var/lib/ump after exporting required audit data.
exit 0
